version: '3.8'

services:
  x-bookmarks:
    build: .
    container_name: x-bookmarks
    restart: unless-stopped
    ports:
      - "3002:3002"
    volumes:
      # Persist database
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - PORT=3002
